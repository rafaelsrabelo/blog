<!DOCTYPE html>
<html>
  <head>
    <title>Blog</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_pack_tag 'application', media: 'all' %>
    <%= javascript_pack_tag 'application' %>
  </head>

  <body> 
    <div class="bg-primary text-white text-center mb-4 p-4">
      <%= link_to root_path, class:"text-white" do %>
        <h1 class="display-4">Blog da programação</h1>
      <% end %>
      <p class="lead">
        Um site de artigos onde programadores profissionais e entusiastas podem debater questões desse mundo incrível. 
      </p>
    </div>
    
    <div class="container">
      <% flash.each do |type, message| %>
        <div class="alert alert-<%= type == 'alert' ? 'danger' : 'success' %> alert-dismissible fade show" role="alert">
          <%= message %>
          <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
      <% end %>

      <div class="row">
        <%= form_with url: search_posts_path, method: :get, local: true, class:"" do |form| %>
          <div class="col-4 offset-8">
                <div class="form-group mx-3 mb-3">
                  <%= form.search_field :q, class:"form-control", required: true, placeholder: 'Buscar...', value: params[:q] %>
                </div>
        <% end %>
        </div>
      </div>

      <%= yield %>
    </div>
    
    <footer class="bg-primary text-white text-center mt-5 p-4">
      Copyright © @rafaelsrabelo <%= Date.current.year %>
    </footer>
  </body>
</html>
